name: tylas_sweet
description: "Reposteria Tylas Sweet"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0 # Bajamos la exigencia del SDK para que GitHub no sufra

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/